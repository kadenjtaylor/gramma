package gramma.model.action;

public enum PartialActionType {
    REMOVE_EDGE_BY_ID,
    REMOVE_NODE_BY_ID,
    ADD_NODE,
    ADD_EDGE,
    ADD_NODE_WITH_LABEL,
    ADD_EDGE_WITH_LABEL,
    REMOVE_EDGE_BY_LABEL,
    REMOVE_NODE_BY_LABEL,
    SET_EDGE_SOURCE,
    SET_EDGE_TARGET;
}